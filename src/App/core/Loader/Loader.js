import connect from 'react-redux';

// orizei global loader
const const Loader = () => ();

const mapStateToProps = state => ({

});

const mapDidpatchToProps = dispatch => ({

});

export default connect(mapStateToProps, mapDidpatchToProps)(Loader);
